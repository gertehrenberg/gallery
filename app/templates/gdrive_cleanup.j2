<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Google Drive Cleanup</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
            line-height: 1.5;
        }
        h1 { margin-bottom: 20px; }
        h2 {
            margin-top: 40px;
            border-bottom: 2px solid #ddd;
            padding-bottom: 5px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 12px;
        }
        th, td {
            padding: 8px 10px;
            border-bottom: 1px solid #ddd;
        }
        th {
            background: #f8f8f8;
            text-align: left;
        }
        .warn { color: #d9534f; font-weight: bold; }
        .ok   { color: #5cb85c; font-weight: bold; }
        .count {
            color: #777;
            font-size: 14px;
            margin-left: 5px;
        }
        .dry {
            color: darkorange;
            font-weight: bold;
        }
        img.thumb {
            height: 60px;
            border: 1px solid #ccc;
            border-radius: 4px;
            object-fit: cover;
        }
    </style>
</head>

<body>
<h1>Google Drive – Case-Duplicate Cleanup</h1>

<p class="dry">Dies ist ein Dry Run. Es wurde nichts gelöscht.</p>

{% for c in categories %}

    <h2>
        {{ c.label }} ({{ c.key }})
        <span class="count">{{ c.num_results }} Duplikate gefunden</span>
    </h2>

    {% if c.num_results == 0 %}
        <p>Keine Duplikate in diesem Ordner.</p>
    {% else %}

        <table>
            <thead>
                <tr>
                    <th>Löschen?</th>
                    <th>Uppercase-Datei</th>
                    <th>Thumbnail<br>Uppercase</th>
                    <th>Thumbnail<br>Lowercase</th>
                    <th>Behaltene Datei</th>
                </tr>
            </thead>

            <tbody>
            {% for r in c.results %}
                <tr>

                    <!-- Uppercase-Datei -->
                    <td class="warn">{{ r.delete }}</td>

                    <!-- Thumbnail der Datei, die gelöscht werden soll -->
                    <td>
                        <img class="thumb"
                             src="https://drive.google.com/thumbnail?id={{ r.delete_id }}&sz=w200"
                             alt="delete">
                    </td>

                    <!-- Checkbox -->
                    <td>
                        <input type="checkbox" name="delete_ids" value="{{ r.delete_id }}">
                    </td>

                    <!-- Thumbnail der Datei, die behalten wird -->
                    <td>
                        <img class="thumb"
                             src="https://drive.google.com/thumbnail?id={{ r.keep_id }}&sz=w200"
                             alt="keep">
                    </td>

                    <!-- Lowercase-Datei -->
                    <td class="ok">{{ r.keep }}</td>

                </tr>
            {% endfor %}
            </tbody>
        </table>

    {% endif %}

{% endfor %}

</body>
</html>
