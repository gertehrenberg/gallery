<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8"/>
    <title>Galerie – Seite {{ page }}</title>

    {% include "image_gallery_local_css.j2" %}
</head>
<body>

<div id="hamburger" style="position: fixed; top: 50px; left: 20px; z-index: 1000">
    <button
            onclick="toggleMenu()"
            style="padding: 8px 12px; font-size: 0.8em"
    >
        ☰
    </button>
    <div id="menu">
        <hr style="margin: 10px 0"/>
        <div class="input-group" style="display: flex; gap: 6px;">
            <input
                    type="text"
                    id="newTextInput"
                    list="textHistory"
                    placeholder="Filtertext eingeben oder auswählen..."
                    style="padding: 6px 10px; border-radius: 5px; width: 360px;"
                    value="{{ filter_text or '' }}"
            />
            <datalist id="textHistory">
                {% for text in last_texts %}
                    <option value="{{ text }}">
                {% endfor %}
            </datalist>
            <button
                    onclick="submitFilterText()"
                    style="padding: 6px 10px; border-radius: 5px;"
            >
                ✓
            </button>
        </div>
        <div class="input-group" style="display: flex; gap: 6px;">
            <input
                    type="text"
                    id="searchInput"
                    list="searchHistory"
                    placeholder="Suchtext eingeben oder auswählen..."
                    style="padding: 6px 10px; border-radius: 5px; width: 360px;"
                    value="{{ search_text or '' }}"
            />
            <datalist id="searchHistory">
                {% for text in search_history %}
                    <option value="{{ text }}">
                {% endfor %}
            </datalist>
            <button
                    onclick="submitSearchText()"
                    style="padding: 6px 10px; border-radius: 5px;"
            >
                ✓
            </button>
        </div>
        <hr style="margin: 10px 0"/>
        <!-- Kategorien-Icons -->
        <div style="display: flex; align-items: center; gap: 8px; margin: 10px 0;">
            {% for k in kategorien %}
                <span title="{{ k.label }}" style="font-size: 1.2em; cursor: pointer;"
                      onclick="selectCategory('{{ k.key }}')">{{ k.icon }}</span>
            {% endfor %}
            <span title="Alles abwählen" style="font-size: 1.2em; cursor: pointer;" onclick="deselectAll()">🗑️</span>
        </div>
        <div style="display: flex; align-items: center; gap: 6px">
            <input
                    id="gotoPageInput"
                    max="{{ total_pages }}"
                    min="1"
                    placeholder="Seite..."
                    style="width: 60px; padding: 4px"
                    type="number"
            />
            <button onclick="gotoPage()">Gehe zu Seite</button>
        </div>
        <hr style="margin: 10px 0"/>
        <div style="display: flex; flex-direction: column; gap: 6px;">
            <div style="display: flex; gap: 6px;">
                <button onclick="dashboard()"
                        style="padding: 6px 10px; border-radius: 5px; width: 180px;">
                    📊 Dashboard
                </button>
            </div>
            {% for k in kategorien %}
                <div style="display: flex; gap: 6px;">
                    <button onclick="moveToFolder({{ kategorien }}, '{{ k.key }}')"
                            style="padding: 6px 10px; border-radius: 5px; width: 180px;">
                        {{ k.icon }} {{ k.label }} (<span id="count-{{ k.key }}">?</span>)
                    </button>
                    <button
                            onclick="navigateToFolder({{ kategorien }}, '{{ k.key }}', {{ count }})"
                            style="padding: 6px 10px; border-radius: 5px; width: 40px; {{ 'background: #eef;' if folder_name == k.key else '' }}">
                        📂
                    </button>
                </div>
            {% endfor %}
            <div style="display: flex; gap: 6px;">
                <button onclick="logout()"
                        style="padding: 6px 10px; border-radius: 5px; width: 180px;">
                    🔒 Logout
                </button>
            </div>
        </div>

        <button class="section-toggle" onclick="toggleConfigSection()">▶ Configuration</button>
        <div id="configSection" class="config-section">
            <form id="configForm">
                <fieldset>
                    <legend>Textanzeige</legend>
                    <label><input type="radio" name="textOption" value="1"{% if textflag == '1' %} checked{% endif %}>
                        keine Anzeige</label><br>
                    <label><input type="radio" name="textOption" value="2"{% if textflag == '2' %} checked{% endif %}>
                        ganzer Text</label><br>
                    <label><input type="radio" name="textOption" value="3"{% if textflag == '3' %} checked{% endif %}>
                        nur erste Zeile</label><br>
                    <label><input type="radio" name="textOption" value="4"{% if textflag == '4' %} checked{% endif %}>
                        kein Englisch</label><br>
                </fieldset>
            </form>
        </div>
    </div>
</div>


<div class="sticky-nav">
    <div class="nav-section nav-left">
        {% if page > 1 %}
            <a href="#"
               class="nav-link"
               onclick="saveAndGo(1, {{ count }}, '{{ folder_name }}')">
                ⏮ Anfang
            </a>
            <a href="#"
               class="nav-link"
               onclick="saveAndGo({{ page - 1 }}, {{ count }}, '{{ folder_name }}')">
                ⬅ Zurück
            </a>
        {% else %}
            <span class="nav-link nav-link-disabled">⏮ Anfang</span>
            <span class="nav-link nav-link-disabled">⬅ Zurück</span>
        {% endif %}
        <button class="nav-btn nav-btn-ghost"
                title="Anzahl halbieren"
                onclick="saveAndGo({{ page }}, {{ (count // 2) if count > 1 else 1 }}, '{{ folder_name }}')">
            ➗2
        </button>
        <button class="nav-btn nav-btn-ghost"
                title="Anzahl -1"
                onclick="saveAndGo({{ page }}, {{ (count - 1) if count > 1 else 1 }}, '{{ folder_name }}')">
            ➖
        </button>
    </div>

    <div class="nav-section nav-center">
        {% if lastcall %}
            <button class="nav-btn nav-btn-icon"
                    title="Zurück zur letzten Ansicht"
                    onclick="window.location.href='{{ lastcall }}'">
                ↩️
            </button>
        {% endif %}

        <span class="nav-folder">
            {{ kategorien | selectattr("key", "eq", folder_name) | map(attribute="label") | first }}
        </span>

        <button class="nav-btn nav-btn-primary"
                onclick="saveAndGoRefresh({{ page }}, {{ count }}, '{{ folder_name }}')">
            {% if filter_text %}⚡{% endif %}{% if search_text %}⚡{% endif %}
            Seite {{ page }} von {{ total_pages }} ({{ total_images }})
        </button>
    </div>

    <div class="nav-section nav-right">
        <div class="nav-count-buttons">
            <button class="nav-btn nav-btn-ghost"
                    title="Anzahl +1"
                    onclick="saveAndGo({{ page }}, {{ count + 1 }}, '{{ folder_name }}')">
                ➕
            </button>
            <button class="nav-btn nav-btn-ghost"
                    title="Anzahl verdoppeln"
                    onclick="saveAndGo({{ page }}, {{ count * 2 }}, '{{ folder_name }}')">
                ✖️2
            </button>
        </div>

        <div class="nav-right-links">
            {% if page < total_pages %}
                <a href="#"
                   class="nav-link"
                   onclick="saveAndGo({{ page + 1 }}, {{ count }}, '{{ folder_name }}')">
                    Weiter ➡
                </a>
                <a href="#"
                   class="nav-link"
                   onclick="saveAndGo({{ total_pages }}, {{ count }}, '{{ folder_name }}')">
                    ⏭ Ende
                </a>
            {% else %}
                <span class="nav-link nav-link-disabled">Weiter ➡</span>
                <span class="nav-link nav-link-disabled">⏭ Ende</span>
            {% endif %}
        </div>
    </div>
</div>

<div class="grid">{{ images_html | safe }}</div>

<!-- Modal-Container -->
<div id="meinModal"
     style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background-color:rgba(0,0,0,0.8); z-index:1000;">
    <!-- statt: <span onclick="closeModal" ... -->
    <button class="close" onclick="closeModal()">×</button>
    <div id="panzoomContainer"
         style="width:100%; height:100%; display:flex; justify-content:center; align-items:center; position:relative; overflow:hidden;">
        <div id="zoomWrapper">
            <img id="zoomImage" src="" style="display: block; max-width: 90vw; max-height: 90vh;"/>
        </div>
        <div id="spinner"></div>
    </div>
</div>

{% include "image_gallery_local_scripts.j2" %}

</body>
</html>
