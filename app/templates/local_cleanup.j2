<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Lokaler Duplikat-Cleanup</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 12px;
        }

        th, td {
            padding: 8px 10px;
            border-bottom: 1px solid #ddd;
            vertical-align: middle;
        }

        th {
            background: #f8f8f8;
            text-align: left;
        }

        img.thumb {
            height: 60px;
            border: 1px solid #ccc;
            border-radius: 4px;
            object-fit: cover;
        }

        .warn { color:#d9534f; font-weight:bold; }
        .warn1 { color:#5bc0de; font-weight:bold; }
        .ok   { color:#5cb85c; font-weight:bold; }

        .center { text-align:center; }

        .btn {
            padding: 8px 16px;
            border-radius: 6px;
            margin-right: 6px;
            border: none;
            cursor: pointer;
            font-weight: 600;
        }
        .btn-blue   { background:#0275d8; color:white; }
        .btn-orange { background:#f0ad4e; color:white; }
        .btn-red    { background:#d9534f; color:white; }
        .btn-cyan   { background:#5bc0de; color:white; }

        .sticky-bar {
            position: sticky;
            top: 0;
            background: #ffffffee;
            padding: 10px 0;
            border-bottom: 1px solid #ddd;
            margin-bottom: 10px;
            backdrop-filter: blur(4px);
        }
    </style>
</head>

<body>

<h1 style="display:flex; justify-content:space-between; align-items:center;">
    <span>Lokaler Duplikat-Cleanup</span>
    <span style="font-size:14px; color:#777;">v{{ version }}</span>
</h1>

<form action="/gallery/local_cleanup_delete" method="post">

<div class="sticky-bar">
    <button type="button" class="btn btn-blue"
            onclick="window.location.href='/gallery/dashboard'">📊 Dashboard</button>

    <button type="button" class="btn btn-orange"
            onclick="window.location.href='/gallery/local_cleanup_reload'">🔄 Neu laden</button>

    <button type="button" class="btn btn-cyan"
            onclick="selectFirst50()">✔️ Erste 50 auswählen</button>

    <button class="btn btn-red" type="submit">🗑 Ausgewählte löschen</button>
</div>

<!-- ======================================================= -->
<!--              EINE EINZIGE MEGA–TABELLE                  -->
<!-- ======================================================= -->

<table>
<thead>
<tr>
    <th>Kategorie</th>
    <th class="center">Löschen-Bild</th>
    <th class="center">✔</th>
    <th class="center">Behalten-Bild</th>
    <th>Löschen/Umbenennen</th>
    <th>Ergebnis</th>
</tr>
</thead>

<tbody>

{% for c in categories %}

    <!-- ======================= -->
    <!--   DUPLICATE RESULTS     -->
    <!-- ======================= -->
    {% for r in c.results %}
        <tr>

            <td><b>{{ c.label }}</b></td>

            <!-- DELETE IMG -->
            <td class="center">
                <img class="thumb"
                     src="/static/imagefiles/{{ r.folder }}/{{ r.delete }}">
            </td>

            <!-- Checkbox -->
            <td class="center">
                <input type="checkbox" name="delete_ids" value="{{ r.delete_id }}">
            </td>

            <!-- KEEP IMG -->
            <td class="center">
                <img class="thumb"
                     src="/static/imagefiles/{{ r.folder }}/{{ r.keep }}">
            </td>

            <td class="warn">{{ r.delete }}</td>
            <td class="ok">{{ r.keep }}</td>
        </tr>
    {% endfor %}

    <!-- ======================= -->
    <!--       WEIRD CASES       -->
    <!-- ======================= -->
    {% for w in c.weird %}
        <tr>
            <td><b>{{ c.label }}</b></td>

            <td></td>
            <td></td>
            <td class="center">
                <img class="thumb"
                     src="/static/imagefiles/{{ w.folder }}/{{ w.name }}">
            </td>

            <td class="warn1">{{ w.name }}</td>
            <td class="ok">{{ w.sanitized }}</td>

        </tr>
    {% endfor %}

{% endfor %}

</tbody>
</table>

</form>

<script>
function selectFirst50() {
    const boxes = document.querySelectorAll("input[type='checkbox'][name='delete_ids']");
    let count = 0;
    boxes.forEach(box => {
        if (count < 50) {
            box.checked = true;
            count++;
        }
    });
}
</script>

</body>
</html>
